#include "pch.h"
#include "Range.h"

/*
template<typename T>
Range<T>::Range(T min, T max) : minValue(min), maxValue(max) {}

template<typename T>
bool Range<T>::isInRange(T value) const
{
	return minValue <= value && value <= maxValue;
}

template<typename T>
T Range<T>::getTheClosestValue(T value) const
{
	if (value < minValue) return minValue;
	if (value > maxValue) return maxValue;
	return value;
}

template<typename T>
T Range<T>::getMinimum() const
{
	return minValue;
}

template<typename T>
T Range<T>::getMaximum() const
{
	return maxValue;
}
*/
